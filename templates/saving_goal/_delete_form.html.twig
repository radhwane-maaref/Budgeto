<form method="post" action="{{ path('app_saving_goal_delete', {'id': saving_goal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ saving_goal.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
